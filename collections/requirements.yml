---
collections:
  # Integração com Terraform Enterprise (configuration_version, run)
  - name: hashicorp.terraform
  # Inventário dinâmico a partir do state do Terraform
  - name: cloud.terraform
  # AWS (snapshots, instance info, ELB targets)
  - name: amazon.aws
  - name: community.aws
  # ITSM
  - name: servicenow.itsm
  # Utilitários (firewalld, selinux)
  - name: ansible.posix
  - name: community.general
